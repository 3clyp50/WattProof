# Optional operator-managed visual extraction. Public fixtures remain keyless.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6

# Required for persistent production sessions. Generate with: openssl rand -hex 32
WATTPROOF_SESSION_SECRET=
